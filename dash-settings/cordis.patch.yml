# dash-settings 的 bundle patch 层：插入自己的 row（node 半边登记 Swift 载荷，
# 浏览器半边改造设置页）。浏览器那半默认惰性——只有 URL 带 ?dash-settings=1
# 时才动页面，普通 `dsh web` 标签页零影响。
- insert:
    - id: dash-settings
      name: dash-settings
