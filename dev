#!/bin/sh
# 本 worktree 的一行启动命令。
#
#   ./dev              装好并启动（profile 随 worktree 走，端口交给 OS 挑）
#   ./dev --port 3080  想要固定端口时
#   ./dev --help       其余选项
#
# 真正的逻辑都在伞包的 bin/surfclam.js 里——这里只是省掉敲路径。
exec node "$(dirname "$0")/surfclam/bin/surfclam.js" "$@"
